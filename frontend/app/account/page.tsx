'use client'
import React from 'react';

interface AccountProps {

};

function Account({}:AccountProps) {
    return (
        <div className="">
            Account
        </div>
    );
};

export default Account;